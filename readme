📌 Вариант 1: Внедрение в Apache Superset через Handlebars
🧩 Где используется:
Проект визуализирует данные об оттоке клиентов и среднемесячных расходах в зависимости от наличия Online TV.
Можно встроить HTML-шаблон в Superset Dashboard с помощью HTML-чарты и движка шаблонов Handlebars.

✅ Как внедрить:
Создайте Dataset в Superset на основе своей таблицы.
Создайте необходимые меры и добавьте их в визуализацию.
Вставьте адаптированный HTML-шаблон из проекта.

Замените переменные Jinja на Handlebars (пример ниже).

🔄 Пример замены Jinja → Handlebars:

<div class="stat-value">{{ data.length }}</div> <!-- вместо {{ clients|length }} -->

{{#each data}}
  <td>{{churn_percent}}</td>
{{/each}}


Добавьте ваш HTML-чарт в Superset Dashboard.

Настройте стили (CSS можно вставить прямо в шаблон или через глобальные стили Superset).

🌐 Вариант 2: Внедрение на сайте через Flask + HTML + CSS
🧩 Где используется:
Локальный веб-сервер, который рендерит HTML с данными из .csv файла.

📁 Структура проекта:
project-folder/
│
├── templates/
│   └── index.html # (ваш HTML-файл с Jinja-шаблонизацией)
├── run.py             # (Flask сервер)
├── train.csv           # (CSV-файл с данными)

🔧 Как запустить:
Установите Flask:
Выполните в терминале: 
pip install flask

Запустите сервер:

python run.py
Откройте http://localhost:5000 в браузере.

📎 Примечания
Вариант с Flask идеально подходит для прототипов, портфолио, презентаций.

Вариант с Superset — для бизнес-внедрения, особенно если уже используется BI-стек.
